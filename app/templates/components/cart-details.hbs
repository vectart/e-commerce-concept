<h4>
  {{material-design-icon name='shopping-cart' size='3x'~}}
  <span>Shopping cart</span>
</h4>

<ul>
  {{#each item in cart}}
    {{cart-item item=item}}
  {{/each}}
</ul>

{{#if cart.length}}
  <p>
    {{format-number cart.total}}
  </p>
{{/if}}
